import Link from "next/link";
import type { TrainerBooking } from "@/api/trainer-bookings.api";
import { dateOnly, money, nextSession, statusClass, statusLabel, timeOnly } from "./trainer-booking-utils";

export function TrainerBookingCard({ booking }: { booking: TrainerBooking }) {
  const session = nextSession(booking);
  return <article className="rounded-xl border border-black/10 bg-white p-5 shadow-sm transition-shadow hover:shadow-md sm:p-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p className="text-[10px] font-semibold uppercase tracking-[0.16em] text-brand-purple">{booking.service.name}</p><h3 className="mt-2 font-serif text-2xl text-brand-dark">{booking.customer.name}</h3><p className="mt-1 text-sm text-brand-gray">{booking.quantity} {booking.quantity === 1 ? "session" : "sessions"} · Booking {booking.id.slice(0, 8)}</p></div><span className={`w-fit rounded-full px-3 py-1.5 text-[10px] font-semibold uppercase tracking-[0.12em] ${statusClass(booking.status)}`}>{statusLabel(booking.status)}</span></div><div className="mt-5 grid gap-3 border-y border-black/5 py-4 text-sm sm:grid-cols-3"><div><p className="text-[10px] uppercase tracking-[0.12em] text-brand-gray">Next session</p><p className="mt-1 font-semibold">{session ? dateOnly(session.scheduledAt) : "Not scheduled"}</p></div><div><p className="text-[10px] uppercase tracking-[0.12em] text-brand-gray">Time</p><p className="mt-1 font-semibold">{session ? timeOnly(session.scheduledAt) : "—"}</p></div><div><p className="text-[10px] uppercase tracking-[0.12em] text-brand-gray">Payment</p><p className="mt-1 font-semibold">{booking.paymentStatus ?? "Pending"}</p></div></div><div className="mt-5 flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><p className="text-xs text-brand-gray">Created {dateOnly(booking.createdAt)} · {money(booking.totalAmount)}</p><Link className="inline-flex min-h-11 items-center justify-center rounded-md bg-brand-purple px-5 text-[10px] font-semibold uppercase tracking-[0.15em] text-white transition-colors hover:bg-brand-dark focus:outline-none focus:ring-2 focus:ring-brand-purple focus:ring-offset-2" href={`/trainer/bookings/${booking.id}`}>View booking</Link></div></article>;
}
