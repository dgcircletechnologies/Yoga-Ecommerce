"use client";

import { CloseIcon, TrashIcon } from "@/components/ui/icons";
import type { AdminService } from "@/types/admin-service";

export function DeleteServiceDialog({ service, onCancel, onConfirm }: { service: AdminService; onCancel: () => void; onConfirm: () => void }) { return <div aria-labelledby="delete-service-title" aria-modal="true" className="fixed inset-0 z-50 flex items-center justify-center bg-brand-dark/45 px-5" onMouseDown={(event) => { if (event.target === event.currentTarget) onCancel(); }} role="dialog"><div className="w-full max-w-md bg-white p-7 text-center shadow-brand sm:p-10" onMouseDown={(event) => event.stopPropagation()}><button aria-label="Close delete confirmation" className="float-right text-brand-gray" onClick={onCancel} type="button"><CloseIcon /></button><div className="mx-auto flex h-12 w-12 items-center justify-center rounded-full bg-red-50 text-red-600"><TrashIcon /></div><h2 className="mt-5 text-2xl" id="delete-service-title">Delete Service?</h2><p className="mt-3 text-sm leading-6 text-brand-gray">Are you sure you want to delete <strong className="text-brand-dark">“{service.name}”</strong>? This action cannot be undone.</p><div className="mt-7 flex flex-col-reverse gap-3 sm:flex-row sm:justify-center"><button className="inline-flex min-h-12 items-center justify-center border border-brand-purple px-7 text-[11px] font-semibold uppercase tracking-[0.18em] text-brand-purple hover:bg-brand-purple hover:text-white" onClick={onCancel} type="button">Cancel</button><button className="inline-flex min-h-12 items-center justify-center bg-red-600 px-7 text-[11px] font-semibold uppercase tracking-[0.18em] text-white hover:bg-red-700" onClick={onConfirm} type="button">Delete</button></div></div></div>; }
