import { ChevronLeftIcon, ChevronRightIcon } from "@/components/ui/icons";

type CustomerPaginationProps = { currentPage: number; totalPages: number; onPageChange: (page: number) => void };

export function CustomerPagination({ currentPage, totalPages, onPageChange }: CustomerPaginationProps) {
  if (totalPages <= 1) return null;
  return <nav aria-label="Customer pages" className="flex flex-wrap items-center justify-between gap-4 border-t border-black/10 px-6 py-5"><button aria-label="Previous page" className="inline-flex min-h-10 items-center gap-2 border border-black/10 px-4 text-[10px] font-semibold uppercase tracking-[0.12em] text-brand-dark transition-colors hover:border-brand-purple hover:text-brand-purple disabled:cursor-not-allowed disabled:opacity-35" disabled={currentPage === 1} onClick={() => onPageChange(currentPage - 1)} type="button"><ChevronLeftIcon /> Previous</button><div className="flex items-center gap-1">{Array.from({ length: totalPages }, (_, index) => index + 1).map((page) => <button aria-current={page === currentPage ? "page" : undefined} aria-label={`Go to page ${page}`} className={`h-9 min-w-9 px-2 text-sm transition-colors ${page === currentPage ? "bg-brand-purple text-white" : "text-brand-gray hover:bg-brand-light-gray hover:text-brand-dark"}`} key={page} onClick={() => onPageChange(page)} type="button">{page}</button>)}</div><button aria-label="Next page" className="inline-flex min-h-10 items-center gap-2 border border-black/10 px-4 text-[10px] font-semibold uppercase tracking-[0.12em] text-brand-dark transition-colors hover:border-brand-purple hover:text-brand-purple disabled:cursor-not-allowed disabled:opacity-35" disabled={currentPage === totalPages} onClick={() => onPageChange(currentPage + 1)} type="button">Next <ChevronRightIcon /></button></nav>;
}
