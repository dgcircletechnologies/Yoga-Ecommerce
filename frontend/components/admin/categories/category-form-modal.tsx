"use client";
import { useEffect, useState } from "react";
import { CloseIcon } from "@/components/ui/icons";
import type { Category } from "@/types/category";
type Props = { category?: Category; isSubmitting?: boolean; onClose: () => void; onSubmit: (category: Category) => void };
export function CategoryFormModal({ category, isSubmitting, onClose, onSubmit }: Props) {
  const editing = Boolean(category); const [values, setValues] = useState({ name: category?.name ?? "", description: category?.description ?? "", image: category?.image ?? "" }); const [error, setError] = useState("");
  useEffect(() => { const close = (event: KeyboardEvent) => { if (event.key === "Escape") onClose(); }; document.addEventListener("keydown", close); document.body.style.overflow = "hidden"; return () => { document.removeEventListener("keydown", close); document.body.style.overflow = ""; }; }, [onClose]);
  function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const name = values.name.trim(); const image = values.image.trim(); if (!name) return setError("Category name is required."); if (name.length > 100) return setError("Category name must be 100 characters or fewer."); if (!image || !/^https?:\/\/.+/.test(image)) return setError("Enter a valid image URL."); if (values.description.length > 500) return setError("Description must be 500 characters or fewer."); onSubmit({ id: category?.id ?? "", name, description: values.description.trim(), productCount: category?.productCount ?? 0, image, createdAt: category?.createdAt ?? "" }); }
  return <div aria-labelledby="category-form-title" aria-modal="true" className="fixed inset-0 z-50 flex items-center justify-center overflow-y-auto bg-brand-dark/45 px-5 py-8" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }} role="dialog"><div className="my-auto w-full max-w-lg bg-white p-7 shadow-brand sm:p-10" onMouseDown={(event) => event.stopPropagation()}><div className="flex items-start justify-between gap-4"><div><p className="text-[10px] font-semibold uppercase tracking-[0.18em] text-brand-purple">Category management</p><h2 className="mt-2 text-3xl" id="category-form-title">{editing ? "Edit Category" : "Add Category"}</h2></div><button aria-label="Close category form" className="inline-flex h-9 w-9 items-center justify-center text-brand-gray hover:bg-brand-light-gray hover:text-brand-dark" onClick={onClose} type="button"><CloseIcon /></button></div><form className="mt-8" onSubmit={submit}><label className="block text-sm font-semibold">Category Name<input aria-invalid={Boolean(error)} className="mt-2 h-12 w-full rounded-md border border-black/10 px-4 text-sm font-normal outline-none focus:border-brand-purple" onChange={(event) => { setValues((current) => ({ ...current, name: event.target.value })); setError(""); }} placeholder="e.g. Meditation" value={values.name} /></label><label className="mt-5 block text-sm font-semibold">Description<textarea className="mt-2 min-h-32 w-full resize-y rounded-md border border-black/10 p-4 text-sm font-normal outline-none focus:border-brand-purple" maxLength={500} onChange={(event) => setValues((current) => ({ ...current, description: event.target.value }))} placeholder="Describe this category" value={values.description} /><span className="mt-1 block text-right text-xs font-normal text-brand-gray">{values.description.length}/500</span></label><label className="mt-5 block text-sm font-semibold">Category Image URL<input className="mt-2 h-12 w-full rounded-md border border-black/10 px-4 text-sm font-normal outline-none focus:border-brand-purple" onChange={(event) => setValues((current) => ({ ...current, image: event.target.value }))} placeholder="https://example.com/category-image.jpg" type="url" value={values.image} /><span className="mt-1 block text-xs font-normal text-brand-gray">Provide an image URL. Uploading will be added later.</span></label>{error && <p className="mt-3 text-xs text-red-600">{error}</p>}<div className="mt-7 flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><button className="inline-flex min-h-12 items-center justify-center border border-brand-purple px-7 text-[11px] font-semibold uppercase tracking-[0.18em] text-brand-purple hover:bg-brand-purple hover:text-white" onClick={onClose} type="button">Cancel</button><button className="inline-flex min-h-12 items-center justify-center bg-brand-purple px-7 text-[11px] font-semibold uppercase tracking-[0.18em] text-white hover:bg-brand-dark disabled:opacity-60" disabled={isSubmitting} type="submit">{isSubmitting ? "Saving…" : editing ? "Save Changes" : "Add Category"}</button></div></form></div></div>;
}
