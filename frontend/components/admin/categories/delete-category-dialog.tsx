"use client";
import { CloseIcon, TrashIcon } from "@/components/ui/icons";
import type { Category } from "@/types/category";
export function DeleteCategoryDialog({ category, isSubmitting, onCancel, onConfirm }: { category: Category; isSubmitting?: boolean; onCancel: () => void; onConfirm: () => void }) { return <div aria-labelledby="delete-category-title" aria-modal="true" className="fixed inset-0 z-50 flex items-center justify-center bg-brand-dark/45 px-5" role="dialog"><div className="w-full max-w-md bg-white p-7 text-center shadow-brand sm:p-10"><button aria-label="Close delete confirmation" className="float-right text-brand-gray" onClick={onCancel} type="button"><CloseIcon /></button><div className="mx-auto flex h-12 w-12 items-center justify-center rounded-full bg-red-50 text-red-600"><TrashIcon /></div><h2 className="mt-5 text-2xl" id="delete-category-title">Delete Category?</h2><p className="mt-3 text-sm leading-6 text-brand-gray">Are you sure you want to delete <strong className="text-brand-dark">“{category.name}”</strong>?</p><div className="mt-7 flex justify-center gap-3"><button className="min-h-12 border border-brand-purple px-7 text-[11px] font-semibold uppercase tracking-[0.18em] text-brand-purple" onClick={onCancel} type="button">Cancel</button><button className="min-h-12 bg-red-600 px-7 text-[11px] font-semibold uppercase tracking-[0.18em] text-white disabled:opacity-60" disabled={isSubmitting} onClick={onConfirm} type="button">{isSubmitting ? "Deleting…" : "Delete"}</button></div></div></div>; }
